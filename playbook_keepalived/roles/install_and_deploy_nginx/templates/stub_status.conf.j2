server {
    listen {{ monitor_port }};
    server_name 127.0.0.1;
    location /nginx_status {
        stub_status on;
        access_log off;
        }
        location = /basic_status {
            stub_status;
        }
}